[package]
name = "dropmultiplier"
version = "1.0.0"
edition = "2024"
publish = false

[lib]
name = "DropMultiplier"
crate-type = ["cdylib"]

[dependencies]
common = { path = "../../shared" }
engine = { path = "../../engine" }
eldenring.workspace = true
fromsoftware-shared.workspace = true
