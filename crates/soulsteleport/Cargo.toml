[package]
name = "soulsteleport"
version = "1.0.0"
edition = "2024"
publish = false

[lib]
name = "SoulsTeleport"
crate-type = ["cdylib"]

[dependencies]
common = { path = "../../shared" }
eldenring.workspace = true
fromsoftware-shared.workspace = true
# DX12 overlay + Dear ImGui (same stack as SoulsChat); only the DX12 backend.
hudhook = { version = "0.9.3", default-features = false, features = ["dx12"] }
